#include <stdio.h>

float cuadrado();
int main()
{
float x, x2;
cuadrado();
printf("EL cuadrado de %f es %f\n", x, x2);
}
float cuadrado()
{
float x, x2;
printf("Introduce el número del que quieras calculcar su cuadrado:");
scanf("%f", &x);

return x2=x*x;
}
